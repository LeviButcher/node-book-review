mixin reviewCard(review = {})
  article.review-card
    header
      a(href=`/review/${review._id}`)
        h5= review.bookName
      time= review.created
    p= review.content.split(' ').slice(0 , 25).join(' ')
